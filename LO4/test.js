// Introduction to Objects

// JavaScript uses objects!
// Strings, numbers, arrays, plus many more..

var myCar = new Car(); // it returns undefined
var myString = new String(); // default objects of JavaSript

mySring = "HELLO";
console.log(myString.length);

console.log(myString.toLowerCase());

var myString2 = "hi there";
console.log(myString2.length);